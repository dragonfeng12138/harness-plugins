// AUTO-GENERATED by build.mjs — do not edit by hand.
// Re-run `node build.mjs` after changing src/, themes/, or skins/.
window.__ModuleLoader__.load({
	id: "dsh-skins",
	factory: (require) => {
		var module = { exports: {} };
		var exports = module.exports;
/**
 * dsh-skins — ACG 外观包（浏览器端）
 *
 * 两条互斥轨道：
 *   - theme 轨道：token 覆盖型主题族，经 themeService.overrideTokens 叠加
 *     { light, dark } 双套值，跟随 DSH「外观」明暗设置自动切换；
 *   - skin  轨道：完整皮肤（body 属性作用域 CSS），替换背景、面板、配色与字体，
 *     明暗变体由 body[data-ds-dark-theme] 选择器承载。
 * 同一时刻至多一条轨道激活；任一选择清退另一轨道。选择经 localStorage 持久化，
 * 刷新自动恢复；插件停用时 teardown() 回收全部副作用。
 *
 * 数据（THEMES / SKINS）由 build.mjs 构建期内联进下方标记位。
 */

const React = require('react')

const THEMES = [{"id":"aurora","label":"极光 / Aurora","preview":{"light":{"background":"#eef8f7","accent":"#0b8278"},"dark":{"background":"#0c1418","accent":"#4fd1c5"}},"tokens":{"--dsw-alias-bg-base":{"light":"#eef8f7","dark":"#0c1418"},"--dsw-alias-bg-layer-1":{"light":"#ffffff","dark":"#101b21"},"--dsw-alias-bg-layer-2":{"light":"#e4f2f1","dark":"#16252c"},"--dsw-alias-bg-layer-3":{"light":"#d8eceb","dark":"#1d3038"},"--dsw-alias-bg-overlay":{"light":"#ffffff","dark":"#101b21"},"--dsw-alias-bg-skeleton":{"light":"rgba(11,130,120,0.12)","dark":"rgba(255,255,255,0.08)"},"--dsw-alias-border-l1":{"light":"#d8e9e8","dark":"rgba(255,255,255,0.07)"},"--dsw-alias-border-l2":{"light":"#b9d6d4","dark":"rgba(255,255,255,0.14)"},"--dsw-alias-border-l3":{"light":"#9cc5c3","dark":"rgba(255,255,255,0.20)"},"--dsw-alias-brand-primary":{"light":"#0b8278","dark":"#4fd1c5"},"--dsw-alias-label-primary":{"light":"#122c2a","dark":"#e8f4f2"},"--dsw-alias-label-secondary":{"light":"#52716e","dark":"#9fb9b5"},"--dsw-alias-label-tertiary":{"light":"#7f9a97","dark":"#75908c"},"--dsw-alias-label-primary-foreground":{"light":"#ffffff","dark":"#0c1418"},"--dsw-alias-state-error-primary":{"light":"#d9455f","dark":"#ff7d95"},"--dsw-alias-state-success-primary":{"light":"#2f9e63","dark":"#5bd694"},"--dsw-alias-state-warn-primary":{"light":"#c98a1e","dark":"#f0c05e"},"--dsw-alias-button-primary-fill":{"light":"#0b8278","dark":"#4fd1c5"},"--dsw-alias-button-primary-hover":{"light":"#096b62","dark":"#6bdcd2"},"--dsw-alias-button-primary-dimmed":{"light":"#d0ebe8","dark":"#1d3038"},"--dsw-alias-button-ghost-active-fill":{"light":"#dff2f0","dark":"#1d3038"},"--dsw-alias-button-ghost-active-hover":{"light":"#cfe9e6","dark":"#243943"},"--dsw-alias-button-ghost-active-border":{"light":"#0b8278","dark":"#4fd1c5"},"--dsw-alias-button-info-fill":{"light":"#0b8278","dark":"#4fd1c5"},"--dsw-alias-button-info-hover":{"light":"#096b62","dark":"#6bdcd2"},"--dsw-alias-interactive-bg-hover":{"light":"rgba(11,130,120,0.08)","dark":"rgba(255,255,255,0.08)"},"--dsw-alias-interactive-bg-active":{"light":"rgba(11,130,120,0.15)","dark":"rgba(255,255,255,0.14)"},"--dsw-alias-markdown-code-block":{"light":"#ecf6f5","dark":"#16252c"},"--dsw-alias-markdown-code-block-banner":{"light":"#e0f0ee","dark":"#1d3038"},"--dsw-alias-markdown-inline-code":{"light":"#d5ebe9","dark":"#243943"},"--dsw-alias-markdown-code-segment-selected":{"light":"#ffffff","dark":"#1d3038"},"--dsw-alias-markdown-code-segment-unselected":{"light":"#ecf6f5","dark":"#101b21"},"--dsw-alias-scrollbar-bg-l1":{"light":"#bfdcd9","dark":"rgba(255,255,255,0.16)"},"--dsw-alias-scrollbar-hover-l1":{"light":"#94c2bf","dark":"rgba(255,255,255,0.28)"},"--dsw-specific-sidebar-fill":{"light":"#eef8f7","dark":"#0c1418"},"--dsw-specific-menu":{"light":"#ffffff","dark":"#16252c"}}},{"id":"cyber","label":"赛博霓虹 / Cyber Neon","preview":{"light":{"background":"#f2f7f9","accent":"#0a7ab0"},"dark":{"background":"#0a0f16","accent":"#22c3e6"}},"tokens":{"--dsw-alias-bg-base":{"light":"#f2f7f9","dark":"#0a0f16"},"--dsw-alias-bg-layer-1":{"light":"#fbfdfe","dark":"#0d141e"},"--dsw-alias-bg-layer-2":{"light":"#e6eef2","dark":"#121c29"},"--dsw-alias-bg-layer-3":{"light":"#dae5eb","dark":"#182534"},"--dsw-alias-bg-overlay":{"light":"#fbfdfe","dark":"#0d141e"},"--dsw-alias-bg-skeleton":{"light":"rgba(10,122,176,0.12)","dark":"rgba(255,255,255,0.08)"},"--dsw-alias-border-l1":{"light":"#dbe6eb","dark":"rgba(255,255,255,0.07)"},"--dsw-alias-border-l2":{"light":"#bccdd6","dark":"rgba(255,255,255,0.14)"},"--dsw-alias-border-l3":{"light":"#9db3bf","dark":"rgba(255,255,255,0.20)"},"--dsw-alias-brand-primary":{"light":"#0a7ab0","dark":"#22c3e6"},"--dsw-alias-label-primary":{"light":"#10222b","dark":"#e8f4f8"},"--dsw-alias-label-secondary":{"light":"#4d6574","dark":"#9db4c0"},"--dsw-alias-label-tertiary":{"light":"#7b8f9b","dark":"#758d9a"},"--dsw-alias-label-primary-foreground":{"light":"#ffffff","dark":"#0a0f16"},"--dsw-alias-state-error-primary":{"light":"#d9455f","dark":"#ff7d95"},"--dsw-alias-state-success-primary":{"light":"#2f9e63","dark":"#5bd694"},"--dsw-alias-state-warn-primary":{"light":"#c98a1e","dark":"#f0c05e"},"--dsw-alias-button-primary-fill":{"light":"#0a7ab0","dark":"#22c3e6"},"--dsw-alias-button-primary-hover":{"light":"#086a9c","dark":"#4fd4ef"},"--dsw-alias-button-primary-dimmed":{"light":"#d5e8f2","dark":"#182534"},"--dsw-alias-button-ghost-active-fill":{"light":"#e0eef6","dark":"#182534"},"--dsw-alias-button-ghost-active-hover":{"light":"#d0e5f0","dark":"#1f2f42"},"--dsw-alias-button-ghost-active-border":{"light":"#0a7ab0","dark":"#22c3e6"},"--dsw-alias-button-info-fill":{"light":"#0a7ab0","dark":"#22c3e6"},"--dsw-alias-button-info-hover":{"light":"#086a9c","dark":"#4fd4ef"},"--dsw-alias-interactive-bg-hover":{"light":"rgba(10,122,176,0.08)","dark":"rgba(255,255,255,0.08)"},"--dsw-alias-interactive-bg-active":{"light":"rgba(10,122,176,0.15)","dark":"rgba(255,255,255,0.14)"},"--dsw-alias-markdown-code-block":{"light":"#eaf2f6","dark":"#121c29"},"--dsw-alias-markdown-code-block-banner":{"light":"#dfeaf0","dark":"#182534"},"--dsw-alias-markdown-inline-code":{"light":"#d5e6ee","dark":"#1f2f42"},"--dsw-alias-markdown-code-segment-selected":{"light":"#ffffff","dark":"#182534"},"--dsw-alias-markdown-code-segment-unselected":{"light":"#eaf2f6","dark":"#0d141e"},"--dsw-alias-scrollbar-bg-l1":{"light":"#c3d6df","dark":"rgba(255,255,255,0.16)"},"--dsw-alias-scrollbar-hover-l1":{"light":"#97b6c4","dark":"rgba(255,255,255,0.28)"},"--dsw-specific-sidebar-fill":{"light":"#eef4f7","dark":"#0a0f16"},"--dsw-specific-menu":{"light":"#fbfdfe","dark":"#121c29"}}},{"id":"sakura","label":"樱语 / Sakura","preview":{"light":{"background":"#fdf4f7","accent":"#c9407a"},"dark":{"background":"#241219","accent":"#f28ab2"}},"tokens":{"--dsw-alias-bg-base":{"light":"#fdf4f7","dark":"#241219"},"--dsw-alias-bg-layer-1":{"light":"#fff8fa","dark":"#2c1720"},"--dsw-alias-bg-layer-2":{"light":"#f7e8ee","dark":"#33202a"},"--dsw-alias-bg-layer-3":{"light":"#f2dfe8","dark":"#3b2731"},"--dsw-alias-bg-overlay":{"light":"#fffafc","dark":"#2c1720"},"--dsw-alias-bg-skeleton":{"light":"rgba(201,64,122,0.10)","dark":"rgba(255,255,255,0.08)"},"--dsw-alias-border-l1":{"light":"#f0dce4","dark":"rgba(255,255,255,0.07)"},"--dsw-alias-border-l2":{"light":"#e3c2d0","dark":"rgba(255,255,255,0.14)"},"--dsw-alias-border-l3":{"light":"#d5a9bd","dark":"rgba(255,255,255,0.20)"},"--dsw-alias-brand-primary":{"light":"#c9407a","dark":"#f28ab2"},"--dsw-alias-label-primary":{"light":"#3a2330","dark":"#f6e7ee"},"--dsw-alias-label-secondary":{"light":"#7a5c6b","dark":"#c4a3b2"},"--dsw-alias-label-tertiary":{"light":"#a08a97","dark":"#977b89"},"--dsw-alias-label-primary-foreground":{"light":"#ffffff","dark":"#241219"},"--dsw-alias-state-error-primary":{"light":"#d9455f","dark":"#ff7d95"},"--dsw-alias-state-success-primary":{"light":"#2f9e63","dark":"#5bd694"},"--dsw-alias-state-warn-primary":{"light":"#c98a1e","dark":"#f0c05e"},"--dsw-alias-button-primary-fill":{"light":"#c9407a","dark":"#f28ab2"},"--dsw-alias-button-primary-hover":{"light":"#b2356b","dark":"#ffa3c2"},"--dsw-alias-button-primary-dimmed":{"light":"#f6d3e0","dark":"#4a2f3c"},"--dsw-alias-button-ghost-active-fill":{"light":"#fbe3ec","dark":"#3b2731"},"--dsw-alias-button-ghost-active-hover":{"light":"#f6d3e0","dark":"#43303a"},"--dsw-alias-button-ghost-active-border":{"light":"#c9407a","dark":"#f28ab2"},"--dsw-alias-button-info-fill":{"light":"#c9407a","dark":"#f28ab2"},"--dsw-alias-button-info-hover":{"light":"#b2356b","dark":"#ffa3c2"},"--dsw-alias-interactive-bg-hover":{"light":"rgba(201,64,122,0.08)","dark":"rgba(255,255,255,0.08)"},"--dsw-alias-interactive-bg-active":{"light":"rgba(201,64,122,0.14)","dark":"rgba(255,255,255,0.14)"},"--dsw-alias-markdown-code-block":{"light":"#f7e9ef","dark":"#33202a"},"--dsw-alias-markdown-code-block-banner":{"light":"#f2dfe8","dark":"#3b2731"},"--dsw-alias-markdown-inline-code":{"light":"#f6d3e0","dark":"#43303a"},"--dsw-alias-markdown-code-segment-selected":{"light":"#ffffff","dark":"#3b2731"},"--dsw-alias-markdown-code-segment-unselected":{"light":"#f7e9ef","dark":"#2c1720"},"--dsw-alias-scrollbar-bg-l1":{"light":"#eccdd9","dark":"rgba(255,255,255,0.16)"},"--dsw-alias-scrollbar-hover-l1":{"light":"#d8a9bc","dark":"rgba(255,255,255,0.28)"},"--dsw-specific-sidebar-fill":{"light":"#fdeef3","dark":"#241219"},"--dsw-specific-menu":{"light":"#fffafc","dark":"#33202a"}}},{"id":"violet","label":"紫罗兰 / Violet Night","preview":{"light":{"background":"#f7f5fc","accent":"#6a3de8"},"dark":{"background":"#14101f","accent":"#9d7bff"}},"tokens":{"--dsw-alias-bg-base":{"light":"#f7f5fc","dark":"#14101f"},"--dsw-alias-bg-layer-1":{"light":"#ffffff","dark":"#191428"},"--dsw-alias-bg-layer-2":{"light":"#efeaf9","dark":"#201a33"},"--dsw-alias-bg-layer-3":{"light":"#e7e0f4","dark":"#282040"},"--dsw-alias-bg-overlay":{"light":"#ffffff","dark":"#191428"},"--dsw-alias-bg-skeleton":{"light":"rgba(106,61,232,0.12)","dark":"rgba(255,255,255,0.08)"},"--dsw-alias-border-l1":{"light":"#e4dcf2","dark":"rgba(255,255,255,0.07)"},"--dsw-alias-border-l2":{"light":"#cdbfe6","dark":"rgba(255,255,255,0.14)"},"--dsw-alias-border-l3":{"light":"#b7a3d9","dark":"rgba(255,255,255,0.20)"},"--dsw-alias-brand-primary":{"light":"#6a3de8","dark":"#9d7bff"},"--dsw-alias-label-primary":{"light":"#231a3a","dark":"#ece7f7"},"--dsw-alias-label-secondary":{"light":"#6a5f85","dark":"#a99fc4"},"--dsw-alias-label-tertiary":{"light":"#9389ab","dark":"#83789f"},"--dsw-alias-label-primary-foreground":{"light":"#ffffff","dark":"#14101f"},"--dsw-alias-state-error-primary":{"light":"#d9455f","dark":"#ff7d95"},"--dsw-alias-state-success-primary":{"light":"#2f9e63","dark":"#5bd694"},"--dsw-alias-state-warn-primary":{"light":"#c98a1e","dark":"#f0c05e"},"--dsw-alias-button-primary-fill":{"light":"#6a3de8","dark":"#9d7bff"},"--dsw-alias-button-primary-hover":{"light":"#5a2fd6","dark":"#b296ff"},"--dsw-alias-button-primary-dimmed":{"light":"#e6defa","dark":"#2c2444"},"--dsw-alias-button-ghost-active-fill":{"light":"#ede6fb","dark":"#282040"},"--dsw-alias-button-ghost-active-hover":{"light":"#e2d8f8","dark":"#2f2749"},"--dsw-alias-button-ghost-active-border":{"light":"#6a3de8","dark":"#9d7bff"},"--dsw-alias-button-info-fill":{"light":"#6a3de8","dark":"#9d7bff"},"--dsw-alias-button-info-hover":{"light":"#5a2fd6","dark":"#b296ff"},"--dsw-alias-interactive-bg-hover":{"light":"rgba(106,61,232,0.08)","dark":"rgba(255,255,255,0.08)"},"--dsw-alias-interactive-bg-active":{"light":"rgba(106,61,232,0.15)","dark":"rgba(255,255,255,0.14)"},"--dsw-alias-markdown-code-block":{"light":"#f1edfb","dark":"#201a33"},"--dsw-alias-markdown-code-block-banner":{"light":"#e9e2f8","dark":"#282040"},"--dsw-alias-markdown-inline-code":{"light":"#e2d8f8","dark":"#2f2749"},"--dsw-alias-markdown-code-segment-selected":{"light":"#ffffff","dark":"#282040"},"--dsw-alias-markdown-code-segment-unselected":{"light":"#f1edfb","dark":"#191428"},"--dsw-alias-scrollbar-bg-l1":{"light":"#d5c8ee","dark":"rgba(255,255,255,0.16)"},"--dsw-alias-scrollbar-hover-l1":{"light":"#b19bd9","dark":"rgba(255,255,255,0.28)"},"--dsw-specific-sidebar-fill":{"light":"#f5f2fc","dark":"#14101f"},"--dsw-specific-menu":{"light":"#ffffff","dark":"#201a33"}}}]
const SKINS = [{"id":"neon-tokyo","name":"霓虹东京","nameEn":"Neon Tokyo","author":"dsh-skins","tagline":"深夜都市 · 品红青霓虹 · 玻璃面板辉光","description":"赛博朋克风格的深夜东京：深蓝紫渐变城市夜空，品红与青色的霓虹辉光点缀在角落，半透明玻璃面板透出城市光晕，扫描线纹理与霓虹描边按钮营造电子都市氛围。原创 CSS 绘制，无外部素材。","tags":["cyberpunk","neon","tokyo","night","city"],"accent":"#ff2ec4","bodyAttr":"data-dsh-skin-neon-tokyo","order":1,"css":"/*\n * neon-tokyo — 霓虹东京\n * Scope: body[data-dsh-skin-neon-tokyo]  = LIGHT variant (dawn city)\n *        body[data-dsh-skin-neon-tokyo][data-ds-dark-theme] = DARK variant (night city)\n * Only original CSS art: gradients, glows, scanlines. No external assets.\n */\n\n/* ---------- shared: fonts ---------- */\nbody[data-dsh-skin-neon-tokyo] {\n  --dsw-font-family: 'Microsoft YaHei', 'PingFang SC', 'Segoe UI', sans-serif;\n  --ds-font-family-code: 'Cascadia Mono', 'JetBrains Mono', Consolas, 'Courier New', monospace;\n}\n\n/* ---------- light: dawn city ---------- */\nbody[data-dsh-skin-neon-tokyo] {\n  color: #1a1b2e;\n  box-sizing: border-box;\n  background-color: #eef0fa;\n  background-image:\n    repeating-linear-gradient(0deg, rgba(26, 27, 46, 0.022) 0 1px, rgba(0, 0, 0, 0) 1px 4px),\n    radial-gradient(circle at 12% 8%, rgba(255, 46, 196, 0.10), rgba(0, 0, 0, 0) 42%),\n    radial-gradient(circle at 88% 18%, rgba(46, 230, 255, 0.12), rgba(0, 0, 0, 0) 40%),\n    radial-gradient(circle at 55% 96%, rgba(124, 77, 255, 0.12), rgba(0, 0, 0, 0) 46%),\n    linear-gradient(168deg, #f4f6ff 0%, #eef0fa 48%, #fbe9f5 100%);\n  padding: 34px 8px 32px;\n}\n\nbody[data-dsh-skin-neon-tokyo] [id=root] {\n  box-sizing: border-box;\n  -webkit-backdrop-filter: blur(6px);\n  background: linear-gradient(rgba(255, 255, 255, 0.72) 0%, rgba(255, 255, 255, 0.62) 110px, rgba(255, 255, 255, 0.42) 100%);\n  border: 1px solid rgba(200, 30, 157, 0.38);\n  border-radius: 14px;\n  box-shadow:\n    0 0 24px rgba(255, 46, 196, 0.10),\n    0 8px 32px rgba(26, 27, 46, 0.16),\n    inset 0 1px rgba(255, 255, 255, 0.9);\n}\n\nbody[data-dsh-skin-neon-tokyo] {\n  --dsw-alias-bg-base: #eef0fa;\n  --dsw-alias-bg-layer-1: #ffffff;\n  --dsw-alias-bg-layer-2: #e6e8f5;\n  --dsw-alias-bg-layer-3: #dcdfef;\n  --dsw-alias-bg-overlay: #ffffff;\n  --dsw-alias-bg-skeleton: rgba(26, 27, 46, 0.06);\n  --dsw-alias-border-l1: rgba(26, 27, 46, 0.06);\n  --dsw-alias-border-l2: rgba(200, 30, 157, 0.30);\n  --dsw-alias-border-l3: rgba(26, 27, 46, 0.12);\n  --dsw-alias-brand-primary: #c81e9d;\n  --dsw-alias-label-primary: #1a1b2e;\n  --dsw-alias-label-secondary: #565a7c;\n  --dsw-alias-label-tertiary: #868aa8;\n  --dsw-alias-label-primary-foreground: #ffffff;\n  --dsw-alias-state-error-primary: #d93a55;\n  --dsw-alias-state-success-primary: #0f9e63;\n  --dsw-alias-state-warn-primary: #b97a12;\n  --dsw-alias-button-primary-fill: #c81e9d;\n  --dsw-alias-button-primary-hover: #c2239a;\n  --dsw-alias-button-primary-dimmed: #f9e2f4;\n  --dsw-alias-button-ghost-active-fill: #f3d9ee;\n  --dsw-alias-button-ghost-active-hover: #ecc6e6;\n  --dsw-alias-button-ghost-active-border: #c81e9d;\n  --dsw-alias-button-info-fill: #0e9bb8;\n  --dsw-alias-button-info-hover: #22b3d2;\n  --dsw-alias-interactive-bg-hover: rgba(200, 30, 157, 0.08);\n  --dsw-alias-interactive-bg-active: rgba(200, 30, 157, 0.15);\n  --dsw-alias-markdown-code-block: #e9ebf7;\n  --dsw-alias-markdown-code-block-banner: #e0e2f2;\n  --dsw-alias-markdown-inline-code: #d8daec;\n  --dsw-alias-markdown-code-segment-selected: #ffffff;\n  --dsw-alias-markdown-code-segment-unselected: #e9ebf7;\n  --dsw-alias-scrollbar-bg-l1: #d3d2e8;\n  --dsw-alias-scrollbar-hover-l1: #b3aed4;\n  --dsw-specific-sidebar-fill: #f2f3fc;\n  --dsw-specific-menu: #ffffff;\n}\n\nbody[data-dsh-skin-neon-tokyo] ::selection {\n  background: rgba(200, 30, 157, 0.35);\n  color: #ffffff;\n}\n\n/* ---------- dark: night city ---------- */\nbody[data-dsh-skin-neon-tokyo][data-ds-dark-theme] {\n  color: #f2f4ff;\n  background-color: #0a0a14;\n  background-image:\n    repeating-linear-gradient(0deg, rgba(255, 255, 255, 0.028) 0 1px, rgba(0, 0, 0, 0) 1px 4px),\n    radial-gradient(circle at 12% 8%, rgba(255, 46, 196, 0.22), rgba(0, 0, 0, 0) 42%),\n    radial-gradient(circle at 88% 18%, rgba(46, 230, 255, 0.18), rgba(0, 0, 0, 0) 40%),\n    radial-gradient(circle at 55% 96%, rgba(124, 77, 255, 0.22), rgba(0, 0, 0, 0) 46%),\n    linear-gradient(168deg, #0d0d20 0%, #0a0a14 48%, #140b26 100%);\n}\n\nbody[data-dsh-skin-neon-tokyo][data-ds-dark-theme] [id=root] {\n  background: linear-gradient(rgba(255, 255, 255, 0.055) 0%, rgba(255, 255, 255, 0.035) 110px, rgba(255, 255, 255, 0.02) 100%);\n  border-color: rgba(255, 46, 196, 0.38);\n  box-shadow:\n    0 0 24px rgba(255, 46, 196, 0.16),\n    0 0 48px rgba(46, 230, 255, 0.10),\n    0 8px 32px rgba(0, 0, 0, 0.6),\n    inset 0 1px rgba(255, 255, 255, 0.10);\n}\n\nbody[data-dsh-skin-neon-tokyo][data-ds-dark-theme] {\n  --dsw-alias-bg-base: #0a0a14;\n  --dsw-alias-bg-layer-1: #12122a;\n  --dsw-alias-bg-layer-2: #181836;\n  --dsw-alias-bg-layer-3: #1f1f42;\n  --dsw-alias-bg-overlay: #14142e;\n  --dsw-alias-bg-skeleton: rgba(255, 255, 255, 0.08);\n  --dsw-alias-border-l1: rgba(255, 255, 255, 0.07);\n  --dsw-alias-border-l2: rgba(255, 46, 196, 0.30);\n  --dsw-alias-border-l3: rgba(255, 255, 255, 0.20);\n  --dsw-alias-brand-primary: #ff2ec4;\n  --dsw-alias-label-primary: #f2f4ff;\n  --dsw-alias-label-secondary: #b9c0dd;\n  --dsw-alias-label-tertiary: #8b93b8;\n  --dsw-alias-label-primary-foreground: #0a0a14;\n  --dsw-alias-state-error-primary: #ff5d7d;\n  --dsw-alias-state-success-primary: #35e0a1;\n  --dsw-alias-state-warn-primary: #ffb84d;\n  --dsw-alias-button-primary-fill: #ff2ec4;\n  --dsw-alias-button-primary-hover: #ff5ad2;\n  --dsw-alias-button-primary-dimmed: #2a1a33;\n  --dsw-alias-button-ghost-active-fill: #1f1f42;\n  --dsw-alias-button-ghost-active-hover: #26264e;\n  --dsw-alias-button-ghost-active-border: #ff2ec4;\n  --dsw-alias-button-info-fill: #2ee6ff;\n  --dsw-alias-button-info-hover: #63edff;\n  --dsw-alias-interactive-bg-hover: rgba(255, 255, 255, 0.08);\n  --dsw-alias-interactive-bg-active: rgba(255, 255, 255, 0.14);\n  --dsw-alias-markdown-code-block: #141430;\n  --dsw-alias-markdown-code-block-banner: #1b1b3a;\n  --dsw-alias-markdown-inline-code: #26264e;\n  --dsw-alias-markdown-code-segment-selected: #1f1f42;\n  --dsw-alias-markdown-code-segment-unselected: #12122a;\n  --dsw-alias-scrollbar-bg-l1: rgba(255, 46, 196, 0.22);\n  --dsw-alias-scrollbar-hover-l1: rgba(255, 46, 196, 0.40);\n  --dsw-specific-sidebar-fill: #0d0d20;\n  --dsw-specific-menu: #14142e;\n}\n\nbody[data-dsh-skin-neon-tokyo][data-ds-dark-theme] ::selection {\n  background: rgba(255, 46, 196, 0.55);\n  color: #ffffff;\n}\n","a11y":"/*\n * neon-tokyo — 霓虹东京 — 可访问性修正\n * 原则：只提升对比度，不改皮肤素材、布局、尺寸、动画。\n *\n * 审计结论（AA ≥ 4.5:1 目标）：\n *  1) 暗态主按钮：深色文字 #0a0a14 on fill #ff2ec4 ≈ 8.9 ✓；info #2ee6ff 对 #0a0a14 ≈ 9.6 ✓。\n *  2) 亮态主按钮：白字 on fill #c81e9d ≈ 5.0 ✓；hover 已在本皮肤内置为 #c2239a（≈ 4.7 ✓）。\n *  3) 暗态/亮态正文、代码块、次级文字均达标，无需其他 override。\n * 本文件保留为显式声明：无未修正项。\n */\n"},{"id":"retro-arcade","name":"复古街机","nameEn":"Retro Arcade","author":"dsh-skins","tagline":"CRT 荧光绿 · 像素网格底纹 · 全等宽街机字体","description":"80 年代街机厅主题：深色 CRT 荧幕底、荧光绿磷光文字与扫描线，网格底纹与像素描边按钮还原点阵质感，全等宽字体栈带来怀旧游戏机的代入感。原创 CSS 绘制，无外部素材。","tags":["retro","arcade","crt","pixel","green"],"accent":"#35ff9d","bodyAttr":"data-dsh-skin-retro-arcade","order":3,"css":"/*\n * retro-arcade — 复古街机\n * Scope: body[data-dsh-skin-retro-arcade]                 = LIGHT variant (daylight terminal)\n *        body[data-dsh-skin-retro-arcade][data-ds-dark-theme] = DARK variant (CRT night)\n * Only original CSS art: grid + scanlines + phosphor glow. No external assets.\n */\n\n/* ---------- shared: fonts (all-monospace arcade vibe) ---------- */\nbody[data-dsh-skin-retro-arcade] {\n  --dsw-font-family: 'Consolas', 'Cascadia Mono', 'Microsoft YaHei', 'PingFang SC', monospace;\n  --ds-font-family-code: 'Cascadia Mono', 'JetBrains Mono', Consolas, 'Courier New', monospace;\n}\n\n/* ---------- light: daylight terminal ---------- */\nbody[data-dsh-skin-retro-arcade] {\n  color: #0b2d1d;\n  box-sizing: border-box;\n  background-color: #eaf6ee;\n  background-image:\n    linear-gradient(rgba(14, 154, 90, 0.055) 1px, rgba(0, 0, 0, 0) 1px),\n    linear-gradient(90deg, rgba(14, 154, 90, 0.055) 1px, rgba(0, 0, 0, 0) 1px),\n    radial-gradient(circle at 85% 12%, rgba(53, 255, 157, 0.14), rgba(0, 0, 0, 0) 40%);\n  background-size: 24px 24px, 24px 24px, auto;\n  padding: 34px 8px 32px;\n}\n\nbody[data-dsh-skin-retro-arcade] [id=root] {\n  box-sizing: border-box;\n  background: rgba(255, 255, 255, 0.88);\n  border: 2px solid #1f7a4d;\n  border-radius: 10px;\n  box-shadow:\n    4px 4px 0 rgba(14, 154, 90, 0.25),\n    0 8px 32px rgba(11, 45, 29, 0.14);\n}\n\nbody[data-dsh-skin-retro-arcade] {\n  --dsw-alias-bg-base: #eaf6ee;\n  --dsw-alias-bg-layer-1: #ffffff;\n  --dsw-alias-bg-layer-2: #e0f0e6;\n  --dsw-alias-bg-layer-3: #d4e9dc;\n  --dsw-alias-bg-overlay: #ffffff;\n  --dsw-alias-bg-skeleton: rgba(14, 154, 90, 0.10);\n  --dsw-alias-border-l1: #cfe4d7;\n  --dsw-alias-border-l2: #9ccdb1;\n  --dsw-alias-border-l3: #6fb58d;\n  --dsw-alias-brand-primary: #0e9a5a;\n  --dsw-alias-label-primary: #0b2d1d;\n  --dsw-alias-label-secondary: #3c6b52;\n  --dsw-alias-label-tertiary: #6d947d;\n  --dsw-alias-label-primary-foreground: #ffffff;\n  --dsw-alias-state-error-primary: #d93a3a;\n  --dsw-alias-state-success-primary: #0e9a5a;\n  --dsw-alias-state-warn-primary: #b97a12;\n  --dsw-alias-button-primary-fill: #0e9a5a;\n  --dsw-alias-button-primary-hover: #0b7f4a;\n  --dsw-alias-button-primary-dimmed: #d2ecdc;\n  --dsw-alias-button-ghost-active-fill: #dff0e5;\n  --dsw-alias-button-ghost-active-hover: #cee6d7;\n  --dsw-alias-button-ghost-active-border: #0e9a5a;\n  --dsw-alias-button-info-fill: #0e9a5a;\n  --dsw-alias-button-info-hover: #0b7f4a;\n  --dsw-alias-interactive-bg-hover: rgba(14, 154, 90, 0.08);\n  --dsw-alias-interactive-bg-active: rgba(14, 154, 90, 0.15);\n  --dsw-alias-markdown-code-block: #e6f3ea;\n  --dsw-alias-markdown-code-block-banner: #dcefe2;\n  --dsw-alias-markdown-inline-code: #d3e9db;\n  --dsw-alias-markdown-code-segment-selected: #ffffff;\n  --dsw-alias-markdown-code-segment-unselected: #e6f3ea;\n  --dsw-alias-scrollbar-bg-l1: #bfdfcb;\n  --dsw-alias-scrollbar-hover-l1: #94c6a7;\n  --dsw-specific-sidebar-fill: #eef8f1;\n  --dsw-specific-menu: #ffffff;\n}\n\nbody[data-dsh-skin-retro-arcade] ::selection {\n  background: rgba(14, 154, 90, 0.30);\n  color: #0b2d1d;\n}\n\n/* ---------- dark: CRT night ---------- */\nbody[data-dsh-skin-retro-arcade][data-ds-dark-theme] {\n  color: #d8ffe9;\n  background-color: #02120a;\n  background-image:\n    repeating-linear-gradient(0deg, rgba(53, 255, 157, 0.032) 0 1px, rgba(0, 0, 0, 0) 1px 3px),\n    linear-gradient(rgba(53, 255, 157, 0.045) 1px, rgba(0, 0, 0, 0) 1px),\n    linear-gradient(90deg, rgba(53, 255, 157, 0.045) 1px, rgba(0, 0, 0, 0) 1px),\n    radial-gradient(circle at 50% 0%, rgba(53, 255, 157, 0.10), rgba(0, 0, 0, 0) 55%);\n  background-size: auto, 24px 24px, 24px 24px, auto;\n}\n\nbody[data-dsh-skin-retro-arcade][data-ds-dark-theme] [id=root] {\n  background: rgba(4, 26, 14, 0.88);\n  border: 2px solid #1f7a4d;\n  border-radius: 10px;\n  box-shadow:\n    0 0 18px rgba(53, 255, 157, 0.18),\n    4px 4px 0 rgba(0, 0, 0, 0.55),\n    inset 0 0 28px rgba(53, 255, 157, 0.05);\n}\n\nbody[data-dsh-skin-retro-arcade][data-ds-dark-theme] {\n  --dsw-alias-bg-base: #02120a;\n  --dsw-alias-bg-layer-1: #04180d;\n  --dsw-alias-bg-layer-2: #062213;\n  --dsw-alias-bg-layer-3: #082c19;\n  --dsw-alias-bg-overlay: #04180d;\n  --dsw-alias-bg-skeleton: rgba(255, 255, 255, 0.08);\n  --dsw-alias-border-l1: rgba(53, 255, 157, 0.12);\n  --dsw-alias-border-l2: rgba(53, 255, 157, 0.28);\n  --dsw-alias-border-l3: rgba(53, 255, 157, 0.42);\n  --dsw-alias-brand-primary: #35ff9d;\n  --dsw-alias-label-primary: #d8ffe9;\n  --dsw-alias-label-secondary: #8fd8b1;\n  --dsw-alias-label-tertiary: #5fa984;\n  --dsw-alias-label-primary-foreground: #02120a;\n  --dsw-alias-state-error-primary: #ff6b6b;\n  --dsw-alias-state-success-primary: #35ff9d;\n  --dsw-alias-state-warn-primary: #ffd35e;\n  --dsw-alias-button-primary-fill: #35ff9d;\n  --dsw-alias-button-primary-hover: #5effb4;\n  --dsw-alias-button-primary-dimmed: #0a3020;\n  --dsw-alias-button-ghost-active-fill: #082c19;\n  --dsw-alias-button-ghost-active-hover: #0a3520;\n  --dsw-alias-button-ghost-active-border: #35ff9d;\n  --dsw-alias-button-info-fill: #35ff9d;\n  --dsw-alias-button-info-hover: #5effb4;\n  --dsw-alias-interactive-bg-hover: rgba(53, 255, 157, 0.08);\n  --dsw-alias-interactive-bg-active: rgba(53, 255, 157, 0.14);\n  --dsw-alias-markdown-code-block: #062213;\n  --dsw-alias-markdown-code-block-banner: #082c19;\n  --dsw-alias-markdown-inline-code: #0a3520;\n  --dsw-alias-markdown-code-segment-selected: #082c19;\n  --dsw-alias-markdown-code-segment-unselected: #04180d;\n  --dsw-alias-scrollbar-bg-l1: rgba(53, 255, 157, 0.20);\n  --dsw-alias-scrollbar-hover-l1: rgba(53, 255, 157, 0.36);\n  --dsw-specific-sidebar-fill: #02120a;\n  --dsw-specific-menu: #062213;\n}\n\nbody[data-dsh-skin-retro-arcade][data-ds-dark-theme] ::selection {\n  background: rgba(53, 255, 157, 0.40);\n  color: #02120a;\n}\n","a11y":"/*\n * retro-arcade — 复古街机 — 可访问性修正\n * 原则：只提升对比度，不改皮肤素材、布局、尺寸、动画。\n *\n * 审计结论（AA ≥ 4.5:1 目标）：\n *  1) 暗态主按钮：深色文字 #02120a on fill #35ff9d ≈ 11.4 ✓。\n *  2) 亮态主按钮：白字 on fill #0e9a5a ≈ 4.8 ✓；hover #0b7f4a ≈ 5.5 ✓。\n *  3) 荧光绿正文 on CRT 深底对比 ≈ 13.6 ✓；亮态深绿正文 on 浅底 ≈ 11.9 ✓。\n * 全部达标，本文件保留为显式声明：无未修正项。\n */\n"},{"id":"sakura-hanami","name":"樱花祭","nameEn":"Sakura Hanami","author":"dsh-skins","tagline":"粉白花见 · 半透明花瓣面板 · 圆润幼圆字体","description":"春日赏樱主题：粉白渐变天空与飘落的花瓣光斑（纯 CSS 径向渐变绘制），半透明花瓣面板透出柔光，圆润的幼圆字体与粉色描边按钮，营造温馨的 ACG 花见氛围。原创绘制，无外部素材。","tags":["sakura","spring","pink","kawaii","hanami"],"accent":"#d9538f","bodyAttr":"data-dsh-skin-sakura-hanami","order":2,"css":"/*\n * sakura-hanami — 樱花祭\n * Scope: body[data-dsh-skin-sakura-hanami]                = LIGHT variant (day hanami)\n *        body[data-dsh-skin-sakura-hanami][data-ds-dark-theme] = DARK variant (petal night)\n * Only original CSS art: radial-gradient petals on a gradient sky. No external assets.\n */\n\n/* ---------- shared: fonts ---------- */\nbody[data-dsh-skin-sakura-hanami] {\n  --dsw-font-family: 'YouYuan', '幼圆', 'Microsoft YaHei', 'PingFang SC', 'Segoe UI', sans-serif;\n  --ds-font-family-code: 'Cascadia Mono', 'JetBrains Mono', Consolas, 'Courier New', monospace;\n}\n\n/* ---------- light: day hanami ---------- */\nbody[data-dsh-skin-sakura-hanami] {\n  color: #4a2b3a;\n  box-sizing: border-box;\n  background-color: #fff7fa;\n  background-image:\n    radial-gradient(circle at 18% 12%, rgba(255, 160, 200, 0.30) 0 7px, rgba(255, 255, 255, 0) 8px),\n    radial-gradient(circle at 30% 22%, rgba(255, 190, 215, 0.26) 0 5px, rgba(255, 255, 255, 0) 6px),\n    radial-gradient(circle at 78% 10%, rgba(255, 170, 205, 0.28) 0 9px, rgba(255, 255, 255, 0) 10px),\n    radial-gradient(circle at 88% 30%, rgba(255, 185, 212, 0.22) 0 6px, rgba(255, 255, 255, 0) 7px),\n    radial-gradient(circle at 12% 70%, rgba(255, 175, 208, 0.20) 0 8px, rgba(255, 255, 255, 0) 9px),\n    radial-gradient(circle at 66% 92%, rgba(255, 190, 216, 0.22) 0 7px, rgba(255, 255, 255, 0) 8px),\n    radial-gradient(circle at 92% 78%, rgba(255, 165, 202, 0.20) 0 6px, rgba(255, 255, 255, 0) 7px),\n    linear-gradient(168deg, #fff9fb 0%, #fff3f7 46%, #ffe9f1 100%);\n  padding: 34px 8px 32px;\n}\n\nbody[data-dsh-skin-sakura-hanami] [id=root] {\n  box-sizing: border-box;\n  -webkit-backdrop-filter: blur(4px);\n  background: linear-gradient(rgba(255, 255, 255, 0.78) 0%, rgba(255, 255, 255, 0.66) 110px, rgba(255, 255, 255, 0.50) 100%);\n  border: 1px solid #f3c6d8;\n  border-radius: 16px;\n  box-shadow:\n    0 8px 32px rgba(217, 83, 143, 0.14),\n    inset 0 1px rgba(255, 255, 255, 0.95);\n}\n\nbody[data-dsh-skin-sakura-hanami] {\n  --dsw-alias-bg-base: #fff7fa;\n  --dsw-alias-bg-layer-1: #ffffff;\n  --dsw-alias-bg-layer-2: #fdeef4;\n  --dsw-alias-bg-layer-3: #f9e2ec;\n  --dsw-alias-bg-overlay: #fffafc;\n  --dsw-alias-bg-skeleton: rgba(217, 83, 143, 0.10);\n  --dsw-alias-border-l1: #f5d9e5;\n  --dsw-alias-border-l2: #ecbad2;\n  --dsw-alias-border-l3: #e09cc0;\n  --dsw-alias-brand-primary: #d9538f;\n  --dsw-alias-label-primary: #4a2b3a;\n  --dsw-alias-label-secondary: #8a5f70;\n  --dsw-alias-label-tertiary: #ad8896;\n  --dsw-alias-label-primary-foreground: #ffffff;\n  --dsw-alias-state-error-primary: #d9455f;\n  --dsw-alias-state-success-primary: #2f9e63;\n  --dsw-alias-state-warn-primary: #c98a1e;\n  --dsw-alias-button-primary-fill: #d9538f;\n  --dsw-alias-button-primary-hover: #c7407f;\n  --dsw-alias-button-primary-dimmed: #fbe0ec;\n  --dsw-alias-button-ghost-active-fill: #fdeaf2;\n  --dsw-alias-button-ghost-active-hover: #fadce9;\n  --dsw-alias-button-ghost-active-border: #d9538f;\n  --dsw-alias-button-info-fill: #d9538f;\n  --dsw-alias-button-info-hover: #c7407f;\n  --dsw-alias-interactive-bg-hover: rgba(217, 83, 143, 0.08);\n  --dsw-alias-interactive-bg-active: rgba(217, 83, 143, 0.15);\n  --dsw-alias-markdown-code-block: #fbebf2;\n  --dsw-alias-markdown-code-block-banner: #f7e0ea;\n  --dsw-alias-markdown-inline-code: #f6d8e5;\n  --dsw-alias-markdown-code-segment-selected: #ffffff;\n  --dsw-alias-markdown-code-segment-unselected: #fbebf2;\n  --dsw-alias-scrollbar-bg-l1: #f0c9db;\n  --dsw-alias-scrollbar-hover-l1: #e2a8c4;\n  --dsw-specific-sidebar-fill: #fff1f6;\n  --dsw-specific-menu: #fffafc;\n}\n\nbody[data-dsh-skin-sakura-hanami] ::selection {\n  background: rgba(217, 83, 143, 0.30);\n  color: #4a2b3a;\n}\n\n/* ---------- dark: petal night ---------- */\nbody[data-dsh-skin-sakura-hanami][data-ds-dark-theme] {\n  color: #f3dce7;\n  background-color: #1e1219;\n  background-image:\n    radial-gradient(circle at 18% 12%, rgba(255, 150, 195, 0.14) 0 7px, rgba(0, 0, 0, 0) 8px),\n    radial-gradient(circle at 30% 22%, rgba(255, 170, 205, 0.12) 0 5px, rgba(0, 0, 0, 0) 6px),\n    radial-gradient(circle at 78% 10%, rgba(255, 160, 198, 0.13) 0 9px, rgba(0, 0, 0, 0) 10px),\n    radial-gradient(circle at 88% 30%, rgba(255, 175, 208, 0.10) 0 6px, rgba(0, 0, 0, 0) 7px),\n    radial-gradient(circle at 12% 70%, rgba(255, 165, 200, 0.10) 0 8px, rgba(0, 0, 0, 0) 9px),\n    radial-gradient(circle at 66% 92%, rgba(255, 180, 212, 0.10) 0 7px, rgba(0, 0, 0, 0) 8px),\n    linear-gradient(168deg, #241019 0%, #1e1219 46%, #2a1522 100%);\n}\n\nbody[data-dsh-skin-sakura-hanami][data-ds-dark-theme] [id=root] {\n  background: linear-gradient(rgba(46, 25, 37, 0.82) 0%, rgba(43, 22, 34, 0.72) 110px, rgba(38, 19, 30, 0.58) 100%);\n  border-color: #5a3048;\n  box-shadow:\n    0 0 28px rgba(255, 140, 190, 0.10),\n    0 8px 32px rgba(0, 0, 0, 0.55),\n    inset 0 1px rgba(255, 255, 255, 0.06);\n}\n\nbody[data-dsh-skin-sakura-hanami][data-ds-dark-theme] {\n  --dsw-alias-bg-base: #1e1219;\n  --dsw-alias-bg-layer-1: #261720;\n  --dsw-alias-bg-layer-2: #2e1d27;\n  --dsw-alias-bg-layer-3: #36232f;\n  --dsw-alias-bg-overlay: #261720;\n  --dsw-alias-bg-skeleton: rgba(255, 255, 255, 0.08);\n  --dsw-alias-border-l1: rgba(255, 255, 255, 0.07);\n  --dsw-alias-border-l2: rgba(255, 150, 195, 0.26);\n  --dsw-alias-border-l3: rgba(255, 255, 255, 0.20);\n  --dsw-alias-brand-primary: #f58ab8;\n  --dsw-alias-label-primary: #f3dce7;\n  --dsw-alias-label-secondary: #c79bb0;\n  --dsw-alias-label-tertiary: #9a7488;\n  --dsw-alias-label-primary-foreground: #1e1219;\n  --dsw-alias-state-error-primary: #ff7d95;\n  --dsw-alias-state-success-primary: #5bd694;\n  --dsw-alias-state-warn-primary: #f0c05e;\n  --dsw-alias-button-primary-fill: #f58ab8;\n  --dsw-alias-button-primary-hover: #ffa3c8;\n  --dsw-alias-button-primary-dimmed: #472c3b;\n  --dsw-alias-button-ghost-active-fill: #36232f;\n  --dsw-alias-button-ghost-active-hover: #3e2a37;\n  --dsw-alias-button-ghost-active-border: #f58ab8;\n  --dsw-alias-button-info-fill: #f58ab8;\n  --dsw-alias-button-info-hover: #ffa3c8;\n  --dsw-alias-interactive-bg-hover: rgba(255, 255, 255, 0.08);\n  --dsw-alias-interactive-bg-active: rgba(255, 255, 255, 0.14);\n  --dsw-alias-markdown-code-block: #2e1d27;\n  --dsw-alias-markdown-code-block-banner: #36232f;\n  --dsw-alias-markdown-inline-code: #3e2a37;\n  --dsw-alias-markdown-code-segment-selected: #36232f;\n  --dsw-alias-markdown-code-segment-unselected: #261720;\n  --dsw-alias-scrollbar-bg-l1: rgba(255, 150, 195, 0.20);\n  --dsw-alias-scrollbar-hover-l1: rgba(255, 150, 195, 0.36);\n  --dsw-specific-sidebar-fill: #1e1219;\n  --dsw-specific-menu: #2e1d27;\n}\n\nbody[data-dsh-skin-sakura-hanami][data-ds-dark-theme] ::selection {\n  background: rgba(245, 138, 184, 0.40);\n  color: #ffffff;\n}\n","a11y":"/*\n * sakura-hanami — 樱花祭 — 可访问性修正\n * 原则：只提升对比度，不改皮肤素材、布局、尺寸、动画。\n *\n * 审计结论（AA ≥ 4.5:1 目标）：\n *  1) 亮态主按钮：白字 on fill #d9538f ≈ 3.9 < 4.5 → 亮态 fill 重定义至 #c7407f（≈ 4.7 ✓）；\n *     hover 本皮肤内置同值，达标。\n *  2) 暗态主按钮：深色文字 #1e1219 on fill #f58ab8 ≈ 8.6 ✓。\n *  3) 正文、代码块、次级文字亮/暗两态均达标，无需其他 override。\n */\nbody[data-dsh-skin-sakura-hanami] {\n  --dsw-alias-button-primary-fill: #c7407f;\n  --dsw-alias-button-info-fill: #c7407f;\n  --dsw-alias-button-info-hover: #b2356f;\n}\n"}]

// ---- localStorage 常量与安全读写 ----
const STORAGE_THEME = 'dsh-skins-theme-v1'
const STORAGE_SKIN = 'dsh-skins-skin-v1'
const STORAGE_TRACK = 'dsh-skins-track-v1'
const STORAGE_FONT_BODY = 'dsh-skins-font-body-v1'
const STORAGE_FONT_CODE = 'dsh-skins-font-code-v1'
const STORAGE_BACKDROP = 'dsh-skins-backdrop-v1'
const STORAGE_GLASS = 'dsh-skins-glass-v1'
const FONT_AUTO = 'auto'

function readStored(key, fallback) {
  try {
    const value = localStorage.getItem(key)
    return value === null || value === '' ? fallback : value
  } catch {
    return fallback
  }
}
function writeStored(key, value) {
  try { localStorage.setItem(key, value) } catch {}
}
function writeStoredResult(key, value) {
  try { localStorage.setItem(key, value); return true } catch { return false }
}

// ---- 持久化：优先 host 设置文档（跨端口/重启存活），localStorage 兜底 + 迁移 ----
const PERSIST_FIELDS = {
  track: { key: STORAGE_TRACK, def: '' },
  themeId: { key: STORAGE_THEME, def: '' },
  skinId: { key: STORAGE_SKIN, def: '' },
  fontBody: { key: STORAGE_FONT_BODY, def: 'auto' },
  fontCode: { key: STORAGE_FONT_CODE, def: 'auto' },
  backdrop: { key: STORAGE_BACKDROP, def: '' },
  glass: { key: STORAGE_GLASS, def: '60' },
}
const saved = {}
for (const [field, spec] of Object.entries(PERSIST_FIELDS)) saved[field] = readStored(spec.key, spec.def)

let persistScope = null
let persistReady = false

function getPersist(field) {
  const value = saved[field]
  return value === undefined || value === null ? PERSIST_FIELDS[field].def : value
}

/** 写入内存 + localStorage（返回是否成功），host 就绪时同步到设置文档。 */
function setPersist(field, value) {
  const next = String(value)
  if (saved[field] === next) return true
  saved[field] = next
  const ok = writeStoredResult(PERSIST_FIELDS[field].key, next)
  if (persistScope !== null && persistReady) {
    persistScope.set(field, next).catch(() => {})
  }
  return ok
}

/** host 设置文档就绪：采纳其中非空值；本地有旧值而宿主为空时迁移写入。 */
function adoptHostState(snapshot) {
  const value = snapshot && snapshot.value
  if (value === undefined || value === null) return
  let changed = false
  for (const field of Object.keys(PERSIST_FIELDS)) {
    const next = value[field]
    if (typeof next === 'string' && next !== '') {
      if (saved[field] !== next) {
        saved[field] = next
        changed = true
      }
    } else if (persistScope !== null && saved[field] !== '' && saved[field] !== PERSIST_FIELDS[field].def) {
      persistScope.set(field, saved[field]).catch(() => {})
    }
  }
  persistReady = true
  if (changed) {
    restoreSelection()
    applyFontLayer()
    applyBackdropLayer()
  }
}

// ---- 字体轨道：独立于主题/皮肤，探测本机已安装字体（Canvas 度量，无权限/无网络） ----
const BODY_FONT_CANDIDATES = [
  { family: 'Microsoft YaHei', label: '微软雅黑 Microsoft YaHei' },
  { family: 'Microsoft YaHei UI', label: 'Microsoft YaHei UI' },
  { family: 'DengXian', label: '等线 DengXian' },
  { family: 'SimSun', label: '宋体 SimSun' },
  { family: 'NSimSun', label: '新宋体 NSimSun' },
  { family: 'SimHei', label: '黑体 SimHei' },
  { family: 'KaiTi', label: '楷体 KaiTi' },
  { family: 'FangSong', label: '仿宋 FangSong' },
  { family: 'YouYuan', label: '幼圆 YouYuan' },
  { family: 'STZhongsong', label: '华文中宋 STZhongsong' },
  { family: 'Segoe UI', label: 'Segoe UI' },
  { family: 'Arial', label: 'Arial' },
  { family: 'Verdana', label: 'Verdana' },
  { family: 'Tahoma', label: 'Tahoma' },
  { family: 'Georgia', label: 'Georgia' },
  { family: 'Times New Roman', label: 'Times New Roman' },
  { family: 'Calibri', label: 'Calibri' },
  { family: 'Cambria', label: 'Cambria' },
  { family: 'Trebuchet MS', label: 'Trebuchet MS' },
  { family: 'Comic Sans MS', label: 'Comic Sans MS' },
]

const CODE_FONT_CANDIDATES = [
  { family: 'Consolas', label: 'Consolas' },
  { family: 'Courier New', label: 'Courier New' },
  { family: 'Cascadia Mono', label: 'Cascadia Mono' },
  { family: 'Cascadia Code', label: 'Cascadia Code' },
  { family: 'JetBrains Mono', label: 'JetBrains Mono' },
  { family: 'Fira Code', label: 'Fira Code' },
  { family: 'Source Code Pro', label: 'Source Code Pro' },
  { family: 'Inconsolata', label: 'Inconsolata' },
  { family: 'MS Gothic', label: 'MS Gothic' },
  { family: 'DejaVu Sans Mono', label: 'DejaVu Sans Mono' },
]

/**
 * 宽度比对法探测字体是否安装：用统一测试串在 sans-serif/serif/monospace
 * 三种泛型回退上分别度量，若把候选字体放在回退前面时宽度发生变化，
 * 则说明候选字体真实参与渲染（已安装）。无权限弹窗、无网络请求。
 */
function createFontDetector() {
  const canvas = document.createElement('canvas')
  const context = canvas.getContext('2d')
  if (context === null) return () => false
  const TEST = 'MiWm字经0123456789-+=|'
  const GENERICS = ['sans-serif', 'serif', 'monospace']
  const baseline = {}
  const width = (font) => {
    context.font = font
    return context.measureText(TEST).width
  }
  for (const generic of GENERICS) baseline[generic] = width(`72px ${generic}`)
  return (family) => {
    for (const generic of GENERICS) {
      if (width(`72px '${family}', ${generic}`) !== baseline[generic]) return true
    }
    return false
  }
}
const detectFont = createFontDetector()
const detectInstalled = (candidates) => candidates.filter((item) => detectFont(item.family))

/** 字体覆盖层：用户选定字体时以 !important 覆盖主题/皮肤的字体变量。 */
let fontLayerTag = null
function applyFontLayer() {
  const body = String(getPersist('fontBody')).replace(/['"]/g, '')
  const code = String(getPersist('fontCode')).replace(/['"]/g, '')
  if (body === FONT_AUTO && code === FONT_AUTO) {
    teardownFontLayer()
    return
  }
  if (fontLayerTag === null) {
    fontLayerTag = document.createElement('style')
    fontLayerTag.setAttribute('data-dsh-skins-font', '')
    document.head.appendChild(fontLayerTag)
  }
  const declarations = []
  if (body !== FONT_AUTO) declarations.push(`--dsw-font-family: '${body}', 'Microsoft YaHei', 'PingFang SC', 'Segoe UI', sans-serif !important;`)
  if (code !== FONT_AUTO) declarations.push(`--ds-font-family-code: '${code}', 'Cascadia Mono', Consolas, 'Courier New', 'Microsoft YaHei', monospace !important;`)
  fontLayerTag.textContent = `body {\n  ${declarations.join('\n  ')}\n}`
}
function teardownFontLayer() {
  if (fontLayerTag !== null) {
    fontLayerTag.remove()
    fontLayerTag = null
  }
}

// ---- 背景图像：预设（原创 SVG 数据 URI，离线可用）+ 自定义 URL + 本地文件 ----
function svgUri(svg) {
  return 'data:image/svg+xml;charset=utf-8,' + encodeURIComponent(svg)
}
function seededRand(seed) {
  return () => {
    seed = (seed * 16807) % 2147483647
    return seed / 2147483647
  }
}

function presetStarfield() {
  const rand = seededRand(20240815)
  const stars = []
  for (let i = 0; i < 48; i++) {
    stars.push(`<circle cx="${(rand() * 1600).toFixed(0)}" cy="${(rand() * 840).toFixed(0)}" r="${(0.7 + rand() * 1.5).toFixed(1)}" fill="#ffffff" opacity="${(0.35 + rand() * 0.65).toFixed(2)}"/>`)
  }
  return svgUri(`<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1600 900"><defs><linearGradient id="s" x1="0" y1="0" x2="0" y2="1"><stop offset="0" stop-color="#0a0f2e"/><stop offset=".55" stop-color="#1c2c60"/><stop offset="1" stop-color="#452a72"/></linearGradient><radialGradient id="n" cx=".8" cy=".15" r=".55"><stop offset="0" stop-color="#7a4dff" stop-opacity=".35"/><stop offset="1" stop-color="#7a4dff" stop-opacity="0"/></radialGradient></defs><rect width="1600" height="900" fill="url(#s)"/><rect width="1600" height="900" fill="url(#n)"/><path d="M150 130 L330 166" stroke="#cfe9ff" stroke-opacity=".85" stroke-width="2" stroke-linecap="round"/><path d="M96 158 L186 168" stroke="#cfe9ff" stroke-opacity=".5" stroke-width="1.4" stroke-linecap="round"/><circle cx="1320" cy="170" r="58" fill="#ffe9f4"/><circle cx="1293" cy="148" r="48" fill="#0a0f2e" opacity=".92"/>${stars.join('')}</svg>`)
}

function presetAurora() {
  const rand = seededRand(42)
  const stars = []
  for (let i = 0; i < 30; i++) {
    stars.push(`<circle cx="${(rand() * 1600).toFixed(0)}" cy="${(rand() * 420).toFixed(0)}" r="${(0.6 + rand() * 1.2).toFixed(1)}" fill="#dff6ff" opacity="${(0.3 + rand() * 0.6).toFixed(2)}"/>`)
  }
  return svgUri(`<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1600 900"><defs><linearGradient id="g" x1="0" y1="0" x2="0" y2="1"><stop offset="0" stop-color="#04121c"/><stop offset="1" stop-color="#0b2f36"/></linearGradient><filter id="b" x="-40%" y="-40%" width="180%" height="180%"><feGaussianBlur stdDeviation="22"/></filter></defs><rect fill="url(#g)" width="1600" height="900"/>${stars.join('')}<g filter="url(#b)" opacity=".55"><path d="M-100 520 Q300 380 700 540 T1600 460" stroke="#35e0a1" stroke-width="26" fill="none"/><path d="M-100 600 Q400 470 800 620 T1600 540" stroke="#2ee6ff" stroke-width="20" fill="none"/><path d="M-100 660 Q500 560 900 680 T1600 620" stroke="#9d7bff" stroke-width="18" fill="none"/></g><path d="M0 760 L240 620 L420 700 L640 560 L880 720 L1100 600 L1340 720 L1600 560 L1600 900 L0 900 Z" fill="#03131b"/></svg>`)
}

function presetSakura() {
  const rand = seededRand(7)
  const petals = []
  for (let i = 0; i < 18; i++) {
    const cx = (rand() * 1600).toFixed(0)
    const cy = (rand() * 900).toFixed(0)
    const angle = (rand() * 360).toFixed(0)
    petals.push(`<ellipse cx="${cx}" cy="${cy}" rx="${(5 + rand() * 7).toFixed(0)}" ry="${(2.5 + rand() * 3.5).toFixed(1)}" fill="#ffb7d5" opacity="${(0.35 + rand() * 0.5).toFixed(2)}" transform="rotate(${angle} ${cx} ${cy})"/>`)
  }
  return svgUri(`<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1600 900"><defs><linearGradient id="g" x1="0" y1="0" x2="0" y2="1"><stop offset="0" stop-color="#fff4f8"/><stop offset=".6" stop-color="#ffe4ee"/><stop offset="1" stop-color="#ffd3e4"/></linearGradient></defs><rect width="1600" height="900" fill="url(#g)"/><circle cx="1280" cy="180" r="90" fill="#ffd9a8" opacity=".85"/>${petals.join('')}</svg>`)
}

function presetPixelSunset() {
  return svgUri(`<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1600 900" shape-rendering="crispEdges"><rect width="1600" height="900" fill="#1a0b2e"/><rect y="0" width="1600" height="120" fill="#3d1a5e"/><rect y="120" width="1600" height="110" fill="#6b2a6e"/><rect y="230" width="1600" height="100" fill="#a93a68"/><rect y="330" width="1600" height="90" fill="#e05a4e"/><rect y="420" width="1600" height="80" fill="#f78f45"/><rect y="500" width="1600" height="400" fill="#2a1140"/><circle cx="1180" cy="300" r="84" fill="#ffd94a"/><g fill="#ffd94a" opacity=".8"><rect x="1060" y="280" width="36" height="40"/><rect x="1004" y="296" width="24" height="8"/><rect x="1276" y="296" width="24" height="8"/><rect x="1264" y="236" width="8" height="24"/></g><g fill="#1a0b2e"><rect x="0" y="560" width="260" height="340"/><rect x="220" y="480" width="300" height="420"/><rect x="480" y="600" width="240" height="300"/><rect x="1240" y="520" width="360" height="380"/></g><g fill="#f78f45" opacity=".5"><rect x="60" y="600" width="12" height="12"/><rect x="140" y="660" width="12" height="12"/><rect x="300" y="540" width="12" height="12"/><rect x="560" y="640" width="12" height="12"/><rect x="1320" y="580" width="12" height="12"/><rect x="1450" y="660" width="12" height="12"/></g></svg>`)
}

function presetNightCity() {
  const rand = seededRand(99)
  const far = []
  let x = 0
  while (x < 1600) {
    const w = 60 + rand() * 90
    const h = 120 + rand() * 160
    far.push(`<rect x="${x.toFixed(0)}" y="${(900 - 320 - h).toFixed(0)}" width="${w.toFixed(0)}" height="${(320 + h).toFixed(0)}" fill="#101a3a"/>`)
    x += w + 4
  }
  const near = []
  const windows = []
  x = 0
  while (x < 1600) {
    const w = 90 + rand() * 80
    const h = 200 + rand() * 260
    const bx = x
    const by = 900 - h
    near.push(`<rect x="${bx.toFixed(0)}" y="${by.toFixed(0)}" width="${w.toFixed(0)}" height="${h.toFixed(0)}" fill="#0a1128"/>`)
    for (let wy = by + 16; wy < 900 - 26; wy += 34) {
      for (let wx = bx + 12; wx < bx + w - 14; wx += 26) {
        if (rand() < 0.2) windows.push(`<rect x="${wx.toFixed(0)}" y="${wy.toFixed(0)}" width="8" height="12" fill="${rand() < 0.5 ? '#ffd94a' : '#2ee6ff'}" opacity="${(0.5 + rand() * 0.5).toFixed(2)}"/>`)
      }
    }
    x += w + 6
  }
  return svgUri(`<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1600 900"><defs><linearGradient id="g" x1="0" y1="0" x2="0" y2="1"><stop offset="0" stop-color="#0a1030"/><stop offset=".6" stop-color="#141c4a"/><stop offset="1" stop-color="#2a1a4a"/></linearGradient></defs><rect width="1600" height="900" fill="url(#g)"/><circle cx="220" cy="150" r="46" fill="#f2e9ff"/><circle cx="205" cy="138" r="40" fill="#0a1030" opacity=".9"/><g opacity=".85">${far.join('')}</g>${near.join('')}${windows.join('')}</svg>`)
}

const BACKDROPS = [
  { id: 'starfield', name: '星野银河', uri: presetStarfield() },
  { id: 'aurora-veil', name: '极光天幕', uri: presetAurora() },
  { id: 'sakura-sky', name: '樱花空', uri: presetSakura() },
  { id: 'pixel-sunset', name: '像素夕阳', uri: presetPixelSunset() },
  { id: 'night-city', name: '夜城霓虹', uri: presetNightCity() },
]

/** `/dsh-skins` RPC 客户端（host 存取背景图文件）；不可用时本地图片降级。 */
let rpcClient = null

/** 解析持久化的背景值：预设 id | url:<raw> | file 的 JSON 串（存路径，不存图像数据）；无效返回 null。 */
function parseBackdrop(value) {
  if (typeof value !== 'string' || value === '') return null
  if (value.startsWith('url:')) {
    const url = value.slice(4)
    if (url === '') return null
    return { kind: 'url', url }
  }
  if (value.startsWith('{')) {
    try {
      const parsed = JSON.parse(value)
      // 新格式只存路径；旧格式（内嵌 data）视为无效，触发首个预设回退。
      if (parsed && parsed.type === 'file' && typeof parsed.path === 'string' && parsed.path !== '') {
        return { kind: 'file', name: typeof parsed.name === 'string' ? parsed.name : '', path: parsed.path }
      }
    } catch {}
    return null
  }
  const preset = BACKDROPS.find((item) => item.id === value)
  return preset ? { kind: 'preset', id: preset.id } : null
}

/** 让 host 把图片落盘，返回 { path, name }；失败返回 null。 */
async function saveBackgroundFile(name, dataUrl) {
  if (rpcClient === null) return null
  try {
    const result = await rpcClient.call('/dsh-skins', 'saveBackground', { name, data: dataUrl })
    if (result && result.ok === true && result.value && typeof result.value.path === 'string') {
      return { path: result.value.path, name: String(result.value.name || name) }
    }
  } catch {}
  return null
}

/** 按路径读回图片 data URL；文件不存在/读失败返回 null。 */
async function readBackgroundFile(path) {
  if (rpcClient === null) return null
  try {
    const result = await rpcClient.call('/dsh-skins', 'readBackground', { path })
    if (result && result.ok === true && result.value && typeof result.value.data === 'string') {
      return result.value.data
    }
  } catch {}
  return null
}

function escapeCssUrl(raw) {
  return String(raw).replace(/(["\\])/g, '\\$1')
}

/** 玻璃化的背景 token 与无主题时的兜底色（明/暗）。 */
const GLASS_TOKENS = [
  { name: '--dsw-alias-bg-base', fallback: { light: '#f9fafb', dark: '#0d131a' } },
  { name: '--dsw-alias-bg-layer-1', fallback: { light: '#ffffff', dark: '#14161c' } },
  { name: '--dsw-alias-bg-layer-2', fallback: { light: '#f0f1f4', dark: '#1d2128' } },
  { name: '--dsw-alias-bg-overlay', fallback: { light: '#ffffff', dark: '#171a20' } },
  { name: '--dsw-specific-sidebar-fill', fallback: { light: '#f4f6f8', dark: '#101318' } },
]

/** 读取玻璃不透明度（0-100，默认 60）。 */
function glassOpacityValue() {
  const stored = parseInt(getPersist('glass'), 10)
  if (!Number.isFinite(stored)) return 60
  return Math.min(100, Math.max(0, stored))
}

/** 设置玻璃不透明度并即时生效。 */
function setGlassOpacity(value) {
  setPersist('glass', value)
  applyBackdropLayer()
  notify()
}

/** 背景层：全屏工作区背景 + 可调玻璃。仅当主题轨道激活时生效；皮肤轨道清退。
 *  文件背景按路径读图；文件缺失/读失败 → 回退第一个预设并自愈持久化值。 */
async function applyBackdropLayer() {
  if (activeTrack !== 'theme') {
    removeBackdropAttr()
    return
  }
  const stored = getPersist('backdrop')
  if (stored === '') {
    removeBackdropAttr()
    return
  }
  let parsed = parseBackdrop(stored)
  if (parsed === null) {
    // 无效/旧格式值：回退第一个预设并自愈。
    const fallback = BACKDROPS[0]
    if (fallback !== undefined) {
      setPersist('backdrop', fallback.id)
      parsed = { kind: 'preset', id: fallback.id }
    } else {
      removeBackdropAttr()
      return
    }
  }
  let raw = null
  if (parsed.kind === 'preset') {
    const preset = BACKDROPS.find((item) => item.id === parsed.id)
    raw = preset === undefined ? null : preset.uri
  } else if (parsed.kind === 'url') {
    raw = parsed.url
  } else {
    raw = await readBackgroundFile(parsed.path)
    let fallbackId = null
    if (raw === null) {
      // 文件不存在或读失败 → 默认使用第一个背景图像并自愈持久化值。
      const fallback = BACKDROPS[0]
      if (fallback === undefined) {
        removeBackdropAttr()
        return
      }
      setPersist('backdrop', fallback.id)
      raw = fallback.uri
      fallbackId = fallback.id
    }
    // 读取期间用户又改了背景 → 放弃本次应用。
    const current = getPersist('backdrop')
    if (current !== stored && current !== fallbackId) return
  }
  if (raw === null) {
    removeBackdropAttr()
    return
  }
  // 玻璃色：取当前主题族各 token 的明/暗值，按滑杆透明度做 color-mix。
  const dark = document.body.hasAttribute('data-ds-dark-theme')
  const family = THEMES.find((item) => item.id === selectedThemeId)
  const opacity = glassOpacityValue()
  document.body.setAttribute('data-dsh-skins-backdrop', '')
  document.body.style.setProperty('--dsk-backdrop-image', `url("${escapeCssUrl(raw)}")`)
  for (const { name, fallback } of GLASS_TOKENS) {
    const token = family && family.tokens[name]
    const color = token && typeof token === 'object' ? token[dark ? 'dark' : 'light'] : null
    const base = typeof color === 'string' && color !== '' ? color : fallback[dark ? 'dark' : 'light']
    const suffix = name.replace('--dsw-alias-', '').replace('--dsw-specific-', '')
    document.body.style.setProperty(`--dsk-glass-${suffix}`, `color-mix(in srgb, ${base} ${opacity}%, transparent)`)
  }
}

function removeBackdropAttr() {
  document.body.removeAttribute('data-dsh-skins-backdrop')
  document.body.style.removeProperty('--dsk-backdrop-image')
  for (const { name } of GLASS_TOKENS) {
    const suffix = name.replace('--dsw-alias-', '').replace('--dsw-specific-', '')
    document.body.style.removeProperty(`--dsk-glass-${suffix}`)
  }
}

/** 持久化背景并立即生效；返回是否成功（本地文件可能超出容量）。 */
function setBackdrop(value) {
  const ok = setPersist('backdrop', value)
  applyBackdropLayer()
  notify()
  return ok
}

// ---- 轨道状态（模块级，apply 注入 service 后可用） ----
let themeService = null
let removeOverride = null
let selectedThemeId = null
let selectedSkinId = null
let activeTrack = '' // 'theme' | 'skin' | ''
let activeView = 'theme' // 'theme' | 'skin' | 'font'（UI 视图，字体视图不与轨道互斥）

const listeners = new Set()
function subscribe(listener) { listeners.add(listener); return () => { listeners.delete(listener) } }
function notify() { for (const listener of listeners) listener() }

const skinStyleSelector = (skinId) => `style[data-dsh-skins="${skinId}"]`

/** 清除当前主题 override 层（若有）。 */
function clearThemeOverride() {
  if (removeOverride !== null) {
    try { removeOverride() } catch {}
    removeOverride = null
  }
}

/** 清除当前皮肤：移除 style 节点与 body 属性。 */
function clearSkin() {
  if (selectedSkinId !== null) {
    const skin = SKINS.find((item) => item.id === selectedSkinId)
    const tag = document.querySelector(skinStyleSelector(selectedSkinId))
    if (tag !== null) tag.remove()
    if (skin && document.body.hasAttribute(skin.bodyAttr)) document.body.removeAttribute(skin.bodyAttr)
  }
  selectedSkinId = null
}

/** 激活一款主题族：清皮肤 → 重挂 token override 层 → 持久化。 */
function activateTheme(themeId) {
  if (themeService === null) return
  const family = THEMES.find((item) => item.id === themeId) || THEMES[0]
  if (family === undefined) return
  clearSkin()
  clearThemeOverride()
  removeOverride = themeService.overrideTokens('dsh-skins', family.tokens)
  selectedThemeId = family.id
  activeTrack = 'theme'
  applyBackdropLayer()
  setPersist('themeId', family.id)
  setPersist('track', 'theme')
  notify()
}

/** 激活一款皮肤：清主题层 → 注入/更换皮肤 CSS → 设置 body 属性 → 持久化。 */
function activateSkin(skinId) {
  const skin = SKINS.find((item) => item.id === skinId)
  if (skin === undefined) return
  clearThemeOverride()
  clearSkin()
  let tag = document.querySelector(skinStyleSelector(skin.id))
  if (tag === null) {
    tag = document.createElement('style')
    tag.setAttribute('data-dsh-skins', skin.id)
    document.head.appendChild(tag)
  }
  tag.textContent = `${skin.css}\n${skin.a11y || ''}`
  document.body.setAttribute(skin.bodyAttr, '')
  selectedSkinId = skin.id
  activeTrack = 'skin'
  applyBackdropLayer()
  setPersist('skinId', skin.id)
  setPersist('track', 'skin')
  notify()
}

/** 恢复 DSH 默认：清空两条轨道的全部效果。 */
function resetTracks() {
  clearThemeOverride()
  clearSkin()
  selectedThemeId = null
  activeTrack = ''
  applyBackdropLayer()
  setPersist('track', '')
  notify()
}

/** 切到主题轨道：恢复上次主题（无记录则第一族）。 */
function openThemeTrack() {
  activeView = 'theme'
  if (activeTrack === 'theme') {
    notify()
    return
  }
  const id = selectedThemeId !== null ? selectedThemeId : (THEMES[0] ? THEMES[0].id : null)
  if (id !== null) {
    activateTheme(id)
  } else {
    activeTrack = 'theme'
    setPersist('track', 'theme')
    notify()
  }
}

/** 切到皮肤轨道：清主题层；曾选过皮肤则恢复。 */
function openSkinTrack() {
  activeView = 'skin'
  if (activeTrack === 'skin') {
    notify()
    return
  }
  clearThemeOverride()
  activeTrack = 'skin'
  applyBackdropLayer()
  setPersist('track', 'skin')
  if (selectedSkinId !== null && SKINS.some((item) => item.id === selectedSkinId)) activateSkin(selectedSkinId)
  else notify()
}

/** 切到字体视图：纯 UI 切换，不动主题/皮肤轨道。 */
function openFontView() {
  activeView = 'font'
  notify()
}

/** apply 时恢复持久化选择；无效 id 回退，首次运行默认第一族。 */
function restoreSelection() {
  const track = getPersist('track')
  if (track === 'skin') {
    const id = getPersist('skinId')
    if (SKINS.some((item) => item.id === id)) {
      activateSkin(id)
      activeView = 'skin'
      return
    }
  }
  if (track === 'theme') {
    const id = getPersist('themeId')
    if (THEMES.some((item) => item.id === id)) {
      activateTheme(id)
      activeView = 'theme'
      return
    }
  }
  if (THEMES.length > 0) activateTheme(THEMES[0].id)
  activeView = 'theme'
}

/** 插件停用：回收全部副作用。 */
function teardown() {
  clearThemeOverride()
  clearSkin()
  teardownFontLayer()
  removeBackdropAttr()
}

// ---- 画廊 UI 样式（自持，随插件卸载移除） ----
const UI_CSS = `
.dsk-wrap { display: grid; gap: 10px; padding: 4px 0; }
.dsk-heading { display: flex; align-items: baseline; justify-content: space-between; gap: 12px; }
.dsk-title { color: var(--dsw-alias-label-primary); font-size: 14px; font-weight: 600; }
.dsk-count { color: var(--dsw-alias-label-secondary); font-size: 12px; }
.dsk-hint { color: var(--dsw-alias-label-secondary); font-size: 12px; line-height: 18px; }
.dsk-tabs { display: flex; gap: 4px; padding: 2px; background: var(--dsw-alias-bg-layer-2); border-radius: 9px; }
.dsk-tab { flex: 1; padding: 6px 10px; border: none; border-radius: 7px; background: transparent; color: var(--dsw-alias-label-secondary); font: inherit; font-size: 12px; cursor: pointer; }
.dsk-tab.is-active { background: var(--dsw-alias-bg-layer-1); color: var(--dsw-alias-label-primary); font-weight: 600; box-shadow: 0 0 0 1px var(--dsw-alias-border-l1); }
.dsk-search { box-sizing: border-box; width: 100%; height: 34px; padding: 0 11px; border: 1px solid var(--dsw-alias-border-l2); border-radius: 9px; outline: none; background: var(--dsw-alias-bg-layer-1); color: var(--dsw-alias-label-primary); font: inherit; font-size: 12px; }
.dsk-search:focus { border-color: var(--dsw-alias-brand-primary); box-shadow: 0 0 0 2px color-mix(in srgb, var(--dsw-alias-brand-primary) 18%, transparent); }
.dsk-root { display: grid; gap: 10px; }
.dsk-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(170px, 1fr)); gap: 10px; padding: 2px; }
.dsk-card { display: grid; grid-template-columns: 34px minmax(0, 1fr); align-items: center; gap: 8px; min-width: 0; padding: 10px; border: 1px solid var(--dsw-alias-border-l1); border-radius: 10px; background: var(--dsw-alias-bg-layer-1); color: var(--dsw-alias-label-primary); cursor: pointer; font: inherit; text-align: left; }
.dsk-card:hover { border-color: var(--dsw-alias-brand-primary); }
.dsk-card.is-active { border-color: var(--dsw-alias-brand-primary); box-shadow: 0 0 0 2px color-mix(in srgb, var(--dsw-alias-brand-primary) 20%, transparent); }
.dsk-swatches { display: grid; grid-template-columns: 1fr 1fr; width: 34px; height: 24px; overflow: hidden; border-radius: 6px; border: 1px solid rgba(127,127,127,.3); }
.dsk-swatches.is-single { grid-template-columns: 1fr; }
.dsk-swatch { position: relative; min-width: 0; }
.dsk-swatch span { position: absolute; right: 2px; bottom: 3px; width: 7px; height: 7px; border-radius: 50%; }
.dsk-copy { min-width: 0; display: grid; gap: 2px; }
.dsk-name { overflow: hidden; text-overflow: ellipsis; white-space: nowrap; font-size: 12px; }
.dsk-meta { color: var(--dsw-alias-label-secondary); font-size: 10px; }
.dsk-empty { padding: 14px; border: 1px dashed var(--dsw-alias-border-l2); border-radius: 10px; color: var(--dsw-alias-label-secondary); text-align: center; font-size: 12px; }
.dsk-font-row { display: grid; grid-template-columns: 96px minmax(0, 1fr); align-items: center; gap: 10px; }
.dsk-font-label { color: var(--dsw-alias-label-secondary); font-size: 12px; }
.dsk-select { box-sizing: border-box; width: 100%; height: 34px; padding: 0 10px; border: 1px solid var(--dsw-alias-border-l2); border-radius: 9px; outline: none; background: var(--dsw-alias-bg-layer-1); color: var(--dsw-alias-label-primary); font: inherit; font-size: 12px; }
.dsk-select:focus { border-color: var(--dsw-alias-brand-primary); box-shadow: 0 0 0 2px color-mix(in srgb, var(--dsw-alias-brand-primary) 18%, transparent); }
.dsk-font-preview { padding: 10px 12px; border: 1px dashed var(--dsw-alias-border-l2); border-radius: 10px; font-size: 13px; color: var(--dsw-alias-label-primary); }
.dsk-font-toolbar { display: flex; align-items: center; gap: 10px; }
.dsk-btn { padding: 6px 12px; border: 1px solid var(--dsw-alias-border-l2); border-radius: 8px; background: var(--dsw-alias-bg-layer-1); color: var(--dsw-alias-label-primary); font: inherit; font-size: 12px; cursor: pointer; }
.dsk-btn:hover { border-color: var(--dsw-alias-brand-primary); }
.dsk-backdrop { display: grid; gap: 8px; padding-top: 10px; border-top: 1px solid var(--dsw-alias-border-l1); }
.dsk-backdrop-title { color: var(--dsw-alias-label-secondary); font-size: 12px; font-weight: 600; }
.dsk-backdrop-row { display: flex; flex-wrap: wrap; gap: 6px; align-items: center; }
.dsk-backdrop-thumb { width: 64px; height: 40px; padding: 0; border: 1px solid var(--dsw-alias-border-l2); border-radius: 8px; background-size: cover; background-position: center; cursor: pointer; overflow: hidden; position: relative; }
.dsk-backdrop-thumb:hover { border-color: var(--dsw-alias-brand-primary); }
.dsk-backdrop-thumb.is-active { border-color: var(--dsw-alias-brand-primary); box-shadow: 0 0 0 2px color-mix(in srgb, var(--dsw-alias-brand-primary) 20%, transparent); }
.dsk-backdrop-thumb.is-named { width: auto; padding: 0 10px; font: inherit; font-size: 11px; color: var(--dsw-alias-label-primary); background: var(--dsw-alias-bg-layer-1); }
.dsk-backdrop-thumb-label { position: absolute; inset: auto 0 0 0; padding: 1px 4px; background: rgba(0,0,0,.45); color: #fff; font-size: 9px; line-height: 13px; text-align: center; }
.dsk-backdrop-url { display: flex; gap: 6px; }
.dsk-glass-row { display: flex; align-items: center; gap: 10px; }
.dsk-glass-row input[type=range] { flex: 1; accent-color: var(--dsw-alias-brand-primary); }
.dsk-error { color: var(--dsw-alias-state-error-primary); font-size: 11px; }
body[data-dsh-skins-backdrop] {
  background-image: var(--dsk-backdrop-image);
  background-size: cover;
  background-position: center;
  /* 工作区全屏玻璃化：各背景 token 指向 JS 按滑杆透明度算好的颜色，
     避免 var() 自引用。!important 压过主题 token 内联值。 */
  --dsw-alias-bg-base: var(--dsk-glass-bg-base) !important;
  --dsw-alias-bg-layer-1: var(--dsk-glass-bg-layer-1) !important;
  --dsw-alias-bg-layer-2: var(--dsk-glass-bg-layer-2) !important;
  --dsw-alias-bg-overlay: var(--dsk-glass-bg-overlay) !important;
  --dsw-specific-sidebar-fill: var(--dsk-glass-sidebar-fill) !important;
}
body[data-dsh-skins-backdrop] [id=root] {
  -webkit-backdrop-filter: blur(7px);
  backdrop-filter: blur(7px);
}
`

// ---- 字体设置面板（settings.section 页签内的字体视图） ----
function FontPanel() {
  const [, force] = React.useReducer((value) => value + 1, 0)
  const [tick, setTick] = React.useState(0)
  const bodyFonts = React.useMemo(() => detectInstalled(BODY_FONT_CANDIDATES), [tick])
  const codeFonts = React.useMemo(() => detectInstalled(CODE_FONT_CANDIDATES), [tick])
  const bodyValue = getPersist('fontBody')
  const codeValue = getPersist('fontCode')

  const pick = (field) => (event) => {
    setPersist(field, event.target.value)
    applyFontLayer()
    force()
  }

  const optionList = (value, fonts) => {
    const entries = fonts.slice()
    if (value !== FONT_AUTO && !entries.some((item) => item.family === value)) {
      entries.unshift({ family: value, label: value })
    }
    return entries
  }

  const picker = (labelText, key, value, fonts) => React.createElement('label', { className: 'dsk-font-row' },
    React.createElement('span', { className: 'dsk-font-label' }, labelText),
    React.createElement('select', { className: 'dsk-select', value, onChange: pick(key) },
      React.createElement('option', { value: FONT_AUTO }, '默认（不覆盖）'),
      ...optionList(value, fonts).map((item) => React.createElement('option', {
        key: item.family, value: item.family, style: { fontFamily: `'${item.family}', sans-serif` },
      }, item.label)),
    ),
  )

  return React.createElement('div', { className: 'dsk-root' },
    React.createElement('div', { className: 'dsk-hint' }, '字体为独立设置：选定后覆盖皮肤自带的字体；选「默认」时皮肤字体生效，无皮肤时使用 DSH 默认字体。列表只显示本机已安装的字体（自动探测，无需联网）。'),
    picker('正文字体', 'fontBody', bodyValue, bodyFonts),
    picker('代码字体', 'fontCode', codeValue, codeFonts),
    React.createElement('div', {
      className: 'dsk-font-preview',
      style: { fontFamily: bodyValue === FONT_AUTO ? undefined : `'${bodyValue}', sans-serif` },
    }, '预览 · ACG 字体示例 · Aa Bb 012 · 深蓝深海深霓虹'),
    React.createElement('div', { className: 'dsk-font-toolbar' },
      React.createElement('button', { type: 'button', className: 'dsk-btn', onClick: () => setTick((value) => value + 1) }, '重新检测字体'),
      React.createElement('span', { className: 'dsk-count' }, `正文 ${bodyFonts.length} 款 · 代码 ${codeFonts.length} 款`),
    ),
  )
}

// ---- 背景图像面板（主题页签内） ----
function BackdropPanel() {
  const [error, setError] = React.useState('')
  const [urlDraft, setUrlDraft] = React.useState('')
  const urlRef = React.useRef(null)
  const parsed = parseBackdrop(getPersist('backdrop'))
  const current = parsed === null ? 'none' : parsed.kind === 'preset' ? parsed.id : 'custom'

  const applyUrl = () => {
    const value = urlDraft.trim()
    if (value === '') return
    if (!/^(https?:\/\/|data:image\/)/i.test(value)) {
      setError('URL 需以 http(s):// 开头，或为 data:image 数据')
      return
    }
    setError(setBackdrop('url:' + value) ? '' : '保存失败：本地存储容量不足')
  }

  const onFile = (event) => {
    const file = event.target.files && event.target.files[0]
    event.target.value = ''
    if (file === undefined) return
    if (!file.type.startsWith('image/')) { setError('请选择图片文件'); return }
    if (file.size > 15728640) { setError('图片过大（>15MB），请压缩后重试或改用 URL'); return }
    const reader = new FileReader()
    reader.onerror = () => setError('读取文件失败')
    reader.onload = async () => {
      const saved = await saveBackgroundFile(file.name, String(reader.result || ''))
      if (saved === null) {
        setError('保存图片失败：图片服务未就绪（host 通道缺失，多为修改后未重启 DSH 应用），请重启后重试，或改用 URL/预设')
        return
      }
      const ok = setBackdrop(JSON.stringify({ type: 'file', name: saved.name, path: saved.path }))
      setError(ok ? '' : '保存失败：本地存储容量不足')
    }
    reader.readAsDataURL(file)
  }

  const thumb = (id, label, title, style) => React.createElement('button', {
    key: id, type: 'button', title,
    className: 'dsk-backdrop-thumb' + (id === 'none' || id === 'custom' ? ' is-named' : '') + (current === id ? ' is-active' : ''),
    'aria-pressed': current === id,
    style,
    onClick: () => {
      if (id === 'none') { setError(setBackdrop('') ? '' : '保存失败'); return }
      if (id === 'custom') {
        if (urlRef.current !== null) urlRef.current.focus()
        return
      }
      setError(setBackdrop(id) ? '' : '保存失败')
    },
  },
    id === 'none' ? '无' : id === 'custom' ? '自定义' : null,
    id !== 'none' && id !== 'custom' ? React.createElement('span', { className: 'dsk-backdrop-thumb-label' }, label) : null,
  )

  // 文件背景只存路径：缩略图不预览（URL/预设才可内联预览）。
  const customStyle = parsed && parsed.kind === 'url'
    ? { backgroundImage: `url("${escapeCssUrl(parsed.url)}")` }
    : {}

  return React.createElement('div', { className: 'dsk-backdrop' },
    React.createElement('div', { className: 'dsk-backdrop-title' }, '背景图像'),
    React.createElement('div', { className: 'dsk-backdrop-row' },
      thumb('none', '无', '不使用背景图像'),
      ...BACKDROPS.map((item) => thumb(item.id, item.name, item.name, { backgroundImage: `url("${escapeCssUrl(item.uri)}")` })),
      thumb('custom', '自定义', '自定义图片（URL 或本地文件）', customStyle),
    ),
    React.createElement('label', { className: 'dsk-font-row' },
      React.createElement('span', { className: 'dsk-font-label' }, '毛玻璃透明度'),
      React.createElement('div', { className: 'dsk-glass-row' },
        React.createElement('input', {
          type: 'range', min: '0', max: '100', step: '5', value: String(glassOpacityValue()), 'aria-label': '毛玻璃透明度',
          onChange: (event) => setGlassOpacity(event.target.value),
        }),
        React.createElement('span', { className: 'dsk-count' }, glassOpacityValue() + '%'),
      ),
    ),
    React.createElement('div', { className: 'dsk-backdrop-url' },
      React.createElement('input', {
        ref: urlRef, className: 'dsk-search', type: 'text', value: urlDraft,
        placeholder: '粘贴图片 URL（https://…）', 'aria-label': '图片 URL',
        onChange: (event) => setUrlDraft(event.target.value),
        onKeyDown: (event) => { if (event.key === 'Enter') applyUrl() },
      }),
      React.createElement('button', { type: 'button', className: 'dsk-btn', onClick: applyUrl }, '应用 URL'),
      React.createElement('label', { className: 'dsk-btn', style: { cursor: 'pointer' } },
        parsed && parsed.kind === 'file' ? '已用本地图：' + parsed.name : '选择本地图片…',
        React.createElement('input', { type: 'file', accept: 'image/*', style: { display: 'none' }, onChange: onFile }),
      ),
    ),
    error !== '' ? React.createElement('div', { className: 'dsk-error' }, error) : null,
    React.createElement('div', { className: 'dsk-hint' }, '背景图为全屏工作区背景（不改变窗口尺寸）；毛玻璃透明度滑杆控制工作区透出程度（0 全透 · 100 不透明）。预设为内置原创图（离线可用），自定义图片保存在浏览器本地。皮肤激活时皮肤自带背景优先，回主题轨道自动恢复。'),
  )
}

// ---- 画廊 React 组件（settings.section 独立页签内容） ----
function Gallery() {
  const [, force] = React.useReducer((value) => value + 1, 0)
  const [query, setQuery] = React.useState('')
  React.useEffect(() => subscribe(force), [])

  const normalized = query.trim().toLowerCase()
  const visibleThemes = THEMES.filter((item) => !normalized || (item.label + ' ' + item.id).toLowerCase().includes(normalized))
  const visibleSkins = SKINS.filter((item) => !normalized || (item.name + ' ' + item.nameEn + ' ' + item.id).toLowerCase().includes(normalized))

  const defaultCard = React.createElement('button', { key: 'dsk-default', type: 'button', className: 'dsk-card', onClick: resetTracks },
    React.createElement('span', { className: 'dsk-swatches' },
      React.createElement('span', { className: 'dsk-swatch', style: { background: '#f9fafb' } }),
      React.createElement('span', { className: 'dsk-swatch', style: { background: '#111318' } }),
    ),
    React.createElement('span', { className: 'dsk-copy' },
      React.createElement('span', { className: 'dsk-name' }, 'DSH 默认'),
      React.createElement('span', { className: 'dsk-meta' }, '恢复默认外观'),
    ),
  )

  const tab = (view, label, onClick) => React.createElement('button', {
    type: 'button', role: 'tab', 'aria-selected': activeView === view,
    className: 'dsk-tab' + (activeView === view ? ' is-active' : ''),
    onClick,
  }, label)

  const tabs = React.createElement('div', { className: 'dsk-tabs', role: 'tablist', 'aria-label': '外观轨道' },
    tab('theme', '主题', openThemeTrack),
    tab('skin', '皮肤', openSkinTrack),
    tab('font', '字体', openFontView),
  )

  const body = activeView === 'skin' ? renderSkinBody() : activeView === 'font' ? React.createElement(FontPanel) : renderThemeBody()

  function renderThemeBody() {
    return React.createElement('div', { className: 'dsk-root' },
      React.createElement('div', { className: 'dsk-hint' }, '配色随明暗外观自动切换；明暗模式请在「外观」行选择，字体在「字体」页签单独设置。'),
      React.createElement('input', {
        className: 'dsk-search', type: 'search', value: query, placeholder: '搜索主题…', 'aria-label': '搜索主题',
        onChange: (event) => setQuery(event.target.value),
      }),
      visibleThemes.length === 0
        ? React.createElement('div', { className: 'dsk-empty' }, '没有匹配的主题')
        : React.createElement('div', { className: 'dsk-grid' },
            defaultCard,
            ...visibleThemes.map((item) => React.createElement('button', {
              key: item.id, type: 'button',
              className: 'dsk-card' + (activeTrack === 'theme' && selectedThemeId === item.id ? ' is-active' : ''),
              'aria-pressed': activeTrack === 'theme' && selectedThemeId === item.id,
              onClick: () => activateTheme(item.id),
            },
              React.createElement('span', { className: 'dsk-swatches' },
                React.createElement('span', { className: 'dsk-swatch', style: { background: item.preview.light.background } }, React.createElement('span', { style: { background: item.preview.light.accent } })),
                React.createElement('span', { className: 'dsk-swatch', style: { background: item.preview.dark.background } }, React.createElement('span', { style: { background: item.preview.dark.accent } })),
              ),
              React.createElement('span', { className: 'dsk-copy' },
                React.createElement('span', { className: 'dsk-name' }, item.label),
                React.createElement('span', { className: 'dsk-meta' }, '明暗双套配色'),
              ),
            )),
          ),
      React.createElement(BackdropPanel),
    )
  }

  function renderSkinBody() {
    return React.createElement('div', { className: 'dsk-root' },
      React.createElement('div', { className: 'dsk-hint' }, '皮肤替换背景、面板、配色与字体；刷新页面自动恢复，明暗变体跟随外观设置。'),
      React.createElement('input', {
        className: 'dsk-search', type: 'search', value: query, placeholder: '搜索皮肤…', 'aria-label': '搜索皮肤',
        onChange: (event) => setQuery(event.target.value),
      }),
      visibleSkins.length === 0
        ? React.createElement('div', { className: 'dsk-empty' }, '没有匹配的皮肤')
        : React.createElement('div', { className: 'dsk-grid' },
            defaultCard,
            ...visibleSkins.map((item) => React.createElement('button', {
              key: item.id, type: 'button',
              className: 'dsk-card' + (activeTrack === 'skin' && selectedSkinId === item.id ? ' is-active' : ''),
              'aria-pressed': activeTrack === 'skin' && selectedSkinId === item.id,
              onClick: () => activateSkin(item.id),
            },
              React.createElement('span', { className: 'dsk-swatches is-single' },
                React.createElement('span', { className: 'dsk-swatch', style: { background: item.accent } }),
              ),
              React.createElement('span', { className: 'dsk-copy' },
                React.createElement('span', { className: 'dsk-name' }, item.name),
                React.createElement('span', { className: 'dsk-meta' }, item.author + ' · ' + item.accent),
              ),
            )),
          ),
    )
  }

  return React.createElement('div', { className: 'dsk-wrap' },
    React.createElement('div', { className: 'dsk-heading' },
      React.createElement('div', { className: 'dsk-title' }, 'ACG 外观'),
      React.createElement('div', { className: 'dsk-count' }, activeView === 'skin' ? SKINS.length + ' 皮肤' : activeView === 'font' ? '独立设置' : THEMES.length + ' 主题'),
    ),
    tabs,
    body,
  )
}

// ---- 插件客户端入口 ----
function apply(ctx) {
  const theme = ctx.get('theme')
  const slots = ctx.get('slots')
  if (theme === undefined || slots === undefined) return
  themeService = theme

  // 恢复持久化选择（首次运行默认第一族）与字体覆盖层。
  restoreSelection()
  applyFontLayer()

  // 明暗切换（theme/change）后重算玻璃色；本包监听器晚于呈现器注册，读取的是已翻转的属性。
  ctx.on('theme/change', () => applyBackdropLayer())

  // 背景图文件存取通道（host RPC）：connection 服务就绪后注入；缺失时本地图片降级。
  ctx.inject(['connection'], (connectionCtx) => {
    rpcClient = connectionCtx.connection.rpc
    applyBackdropLayer()
  })

  // host 设置文档持久化：就绪后采纳已保存状态（主题/皮肤/字体/背景/玻璃），本地旧值自动迁移。
  const scopeService = ctx.get('settingsScope')
  if (scopeService !== undefined) {
    try {
      persistScope = scopeService.bind({ namespace: 'dsh-skins' })
      const sync = () => {
        const snapshot = persistScope.getSnapshot()
        if (snapshot.status === 'ready') adoptHostState(snapshot)
      }
      ctx.effect(() => persistScope.subscribe(sync))
      sync()
    } catch {}
  }

  // 插件停用：回收 override 层、皮肤 style 与 body 属性。
  ctx.effect(() => () => teardown())

  // 画廊 UI 样式：自持 style 节点，随插件卸载移除。
  ctx.effect(() => {
    const tag = document.createElement('style')
    tag.setAttribute('data-dsh-skins-ui', '')
    tag.textContent = UI_CSS
    document.head.appendChild(tag)
    return () => tag.remove()
  })

  // 设置面板：独立「ACG 外观」页签（紧随「常规」之后，不占用通用设置行）。
  slots.inject('settings.section', () => slots.register(
    { name: 'settings.section', id: 'dsh-skins', order: 5, label: 'ACG 外观' },
    Gallery,
  ))
}

		module.exports = { apply };
		return module.exports;
	}
});
